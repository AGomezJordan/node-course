// console.log(process.env);

const { SHELL, EDITOR } = process.env;
// console.table({SHELL, EDITOR})

const characters = [ 'flash', 'superman', 'Spiderman', 'batman' ]

const [,,,batman] = characters

// console.log(batman)
